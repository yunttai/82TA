# Provider Core Scope

- Provider ports, adapters, envelope, schema validation, timeout, retry, circuit breaker, cache, quota, fixtures.
- Never log keys; use fixed endpoint allowlists.
- Distinguish missing, null, zero, stale, unsupported, and error.
- Unverified capabilities cannot become required production dependencies.
