# Routing Domain Scope

- Framework-independent routing domain.
- Use canonical objects, not Provider raw JSON or Django ORM.
- Preserve sequential time-dependent evaluation, strict taxi upper budget, transfer feasibility, bounded candidates, Pareto invariants, and deterministic replay.
