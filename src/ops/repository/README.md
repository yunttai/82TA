# Repository Operations

`CODEOWNERS.template`과 `PULL_REQUEST_TEMPLATE.md`는 저장소 활성화 시 사용하는 운영 원본이다. 제품 파일 `src/` 정책 때문에 여기에서 관리하며, GitHub가 요구하는 root `.github/` 배치가 필요할 경우 ADR과 source-layout allowlist를 갱신한다.
